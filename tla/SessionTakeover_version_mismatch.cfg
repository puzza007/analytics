\* QUESTION: a deploy that touches any of the four md5-hashed modules, so
\* session_version differs and handle_replica returns [].
\* EXPECTED: VIOLATION of LatchMeansTransfer - request_takeover/1 still sends
\* :done from its `after` block, so the primary drops its 15s hold having
\* transferred nothing.
SPECIFICATION Spec

CONSTANTS
    Partitions = {p1, p2}
    MaxEvents  = 3
    NULL       = NULL
    VersionMatches          = FALSE
    OldAttempted            = TRUE
    DoneAlways              = TRUE
    NewServesDuringTakeover = TRUE
    DrainBeforeTakeover     = FALSE
    AwaitCapFires           = FALSE
    PullAfterDrain          = FALSE
    PutNew                  = FALSE

INVARIANT TypeOK
INVARIANT LatchMeansTransfer
INVARIANT LatchMeansCompleteTransfer
INVARIANT NoLostSession
INVARIANT NoSessionSplit
