\* QUESTION: candidate fix - send :done only when a dump actually arrived,
\* instead of unconditionally from the `after` block.
\* EXPECTED: LatchMeansTransfer holds, but the 15s Alive cap still fires, so
\* sessions are still lost - just after a longer wait. Fixing the `after` block
\* alone makes the failure honest, it does not recover the sessions.
SPECIFICATION Spec

CONSTANTS
    Partitions = {p1, p2}
    MaxEvents  = 3
    NULL       = NULL
    VersionMatches          = FALSE
    OldAttempted            = TRUE
    DoneAlways              = FALSE
    NewServesDuringTakeover = TRUE
    DrainBeforeTakeover     = FALSE
    AwaitCapFires           = FALSE
    PullAfterDrain          = FALSE
    PutNew                  = FALSE

INVARIANT TypeOK
INVARIANT LatchMeansTransfer
INVARIANT NoLostSession
INVARIANT NoSessionSplit
