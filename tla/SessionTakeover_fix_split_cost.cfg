\* QUESTION: what does the fix cost? insert_new drops the imported copy when
\* this node already owns a session for the visitor, and pre-drain the two
\* nodes serve independently.
\* EXPECTED: VIOLATION of NoSessionSplit and NoLostSession - the visitor
\* splits across the deploy instead of continuing. Benign next to a double
\* cancel: the dropped copy's final +1 row stands as a correctly terminated
\* visit, so no aggregate is corrupted (EventsCorrect holds in the fix cfg).
SPECIFICATION Spec

CONSTANTS
    Partitions = {p1, p2}
    MaxEvents  = 3
    NULL       = NULL
    VersionMatches          = TRUE
    OldAttempted            = TRUE
    DoneAlways              = TRUE
    NewServesDuringTakeover = TRUE
    DrainBeforeTakeover     = TRUE
    AwaitCapFires           = FALSE
    PullAfterDrain          = TRUE
    PutNew                  = TRUE

INVARIANT NoSessionSplit
INVARIANT NoLostSession
