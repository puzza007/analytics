\* QUESTION: does the fix also close the abandoned fan-out (finding 5)?
\* EXPECTED: VIOLATION of NoPartialTakeover still - Task.await_many's cap and
\* the after-block :done are a separate defect. The fix makes each pulled
\* partition CORRECT (post-drain, insert_new), but not the all-or-nothing
\* accounting of the fan-out itself.
SPECIFICATION Spec

CONSTANTS
    Partitions = {p1, p2}
    MaxEvents  = 3
    NULL       = NULL
    VersionMatches          = TRUE
    OldAttempted            = TRUE
    DoneAlways              = TRUE
    NewServesDuringTakeover = TRUE
    DrainBeforeTakeover     = TRUE
    AwaitCapFires           = TRUE
    PullAfterDrain          = TRUE
    PutNew                  = TRUE

INVARIANT NoDoubleCancel
INVARIANT EventsCorrect
INVARIANT NoPartialTakeover
