\* QUESTION: the candidate fix - the primary declines dumps until it is
\* draining (endpoint stops before Transfer in supervision shutdown order),
\* the replica polls until then, and the restore keeps locally-owned sessions
\* (insert_new). The new node serves traffic the whole time.
\* EXPECTED: PASS on every sign-accounting invariant AND NoTrafficGap - the
\* correctness of drain-first without its availability cost. Splits are the
\* accepted price (own config). Findings 4/5 (latch/after-block) untouched.
SPECIFICATION Spec

CONSTANTS
    Partitions = {p1, p2}
    MaxEvents  = 3
    NULL       = NULL
    VersionMatches          = TRUE
    OldAttempted            = TRUE
    DoneAlways              = TRUE
    NewServesDuringTakeover = TRUE
    DrainBeforeTakeover     = TRUE
    AwaitCapFires           = FALSE
    PullAfterDrain          = TRUE
    PutNew                  = TRUE

INVARIANT TypeOK
INVARIANT NoDoubleCancel
INVARIANT EventsCorrect
INVARIANT NoConcurrentOwnership
INVARIANT NoTrafficGap
INVARIANT LatchMeansTransfer
INVARIANT LatchMeansCompleteTransfer
INVARIANT NoPartialTakeover
