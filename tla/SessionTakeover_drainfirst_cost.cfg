\* QUESTION: what does drain-before-dump cost? It removes the corruption by
\* refusing traffic instead.
\* EXPECTED: VIOLATION of NoTrafficGap. Stated rather than assumed.
SPECIFICATION Spec

CONSTANTS
    Partitions = {p1, p2}
    MaxEvents  = 3
    NULL       = NULL
    VersionMatches          = TRUE
    OldAttempted            = TRUE
    DoneAlways              = TRUE
    NewServesDuringTakeover = FALSE
    DrainBeforeTakeover     = TRUE
    AwaitCapFires           = FALSE
    PullAfterDrain          = FALSE
    PutNew                  = FALSE

INVARIANT NoTrafficGap
