\* QUESTION: can the new node fail to ever serve? A fix that never becomes
\* ready would satisfy every safety property by taking the site down.
\* EXPECTED: PASS - replica and the per-partition tasks are fair.
SPECIFICATION Spec

CONSTANTS
    Partitions = {p1, p2}
    MaxEvents  = 3
    NULL       = NULL
    VersionMatches          = FALSE
    OldAttempted            = FALSE
    DoneAlways              = FALSE
    NewServesDuringTakeover = FALSE
    DrainBeforeTakeover     = TRUE
    AwaitCapFires           = TRUE

PROPERTY NewNodeEventuallyServes
