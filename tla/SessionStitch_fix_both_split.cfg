\* QUESTION: with both fixes applied, is the visitor still split into two
\* sessions across a salt rotation?
\* EXPECTED: VIOLATION of NoSessionSplit. Serialising the keys and re-keying
\* coherently fixes the SIGN ACCOUNTING but not the case where two requests
\* both find no session and both create one. Benign next to the double cancel:
\* it inflates visit counts without corrupting any collapsed aggregate.
SPECIFICATION Spec

CONSTANTS
    Requests = {r1, r2, r3}
    MaxEpoch = 2
    NULL     = NULL
    SaltRotates      = TRUE
    CallerTimeout    = FALSE
    FixLockBothKeys  = TRUE
    FixRekeyCoherent = TRUE
    FixLockOnMissOnly         = FALSE
    FixSaltIndependentRouting = FALSE
    FixNoCrossRotationStitch  = FALSE

INVARIANT NoSessionSplit
